#!/bin/sh
set -eu

# Fast pre-commit checks only (~1s total). Heavy checks (gosec, govulncheck,
# full gitleaks history/working-tree scans) are on-demand via:
#   ./install.sh --security
make fmt-check
make security-secrets-staged
